DATABASE_URL=postgresql://user:password@localhost:5432/mydb
PORT=3000
NODE_ENV=development
# JWT signing secret — auto-generated at startup if not set, but tokens will be
# invalidated on every restart. Set a persistent secret for production.
JWT_SECRET=
